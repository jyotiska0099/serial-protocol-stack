# serial-protocol-stack
A layered serial communication protocol stack from scratch in  C++. No physical hardware. The physical layer will be simulated  using a POSIX socket loopback pair (or a pipe). The goal is to understand how  real embedded communication protocols are architected and tested.
